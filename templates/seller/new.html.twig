{% extends 'layout/dashboard.html.twig' %}

{% block page_title %}New Seller{% endblock %}

{% block content %}
    <div class="mb-6">
        <a href="{{ path('seller_index') }}" class="text-blue-600 hover:underline dark:text-blue-500 text-sm">&larr; Back to sellers</a>
    </div>

    <twig:Card>
        {% block content %}
            {{ form_start(form) }}
                {{ form_rest(form) }}
            {{ form_end(form) }}
        {% endblock %}
    </twig:Card>
{% endblock %}
